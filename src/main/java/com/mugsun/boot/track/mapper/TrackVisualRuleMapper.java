package com.mugsun.boot.track.mapper;

import com.mugsun.boot.track.entity.TrackVisualRule;
import com.mybatisflex.core.BaseMapper;

/**
 * 圈选式可视化埋点规则 Mapper（track 库，经 @TrackDS 路由）。
 */
public interface TrackVisualRuleMapper extends BaseMapper<TrackVisualRule> {
}
