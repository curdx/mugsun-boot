package com.mugsun.boot.track.mapper;

import com.mugsun.boot.track.entity.TrackIdentity;
import com.mybatisflex.core.BaseMapper;

/**
 * 匿名↔登录身份映射 Mapper（track 库，经 @TrackDS 路由）。
 */
public interface TrackIdentityMapper extends BaseMapper<TrackIdentity> {
}
